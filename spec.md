# 服装爆款预测AI工具 · 设计规格说明书 v2.0

> 项目代号：Virtual Consumer Review v2  
> 品类：6-14岁童装户外功能性服饰  
> 阶段：种子品牌内部定制交付（先A后B模式）  
> 文档状态：待用户审阅

---

## 1. 项目概述

### 1.1 背景与目标

在童装开发内审环节，设计师与电商运营需对款式进行S/A/P三级分级决策。当前决策依赖人工经验，存在主观偏差大、可重复性差、无法量化等问题。本工具通过AI模拟不同人设的消费者购买决策，为内审提供**科学可被验证的辅助分析结果**。

### 1.2 核心交付价值

- 在新款式内审前4-6周，输入款式图片+FAB说明，AI输出S/A+/A/P分级建议
- 输出双渠道适配性（自然流量/直播带货）评分
- 对A款提供精准改款方向建议
- 输出质性洞察（消费者决策理由）而非黑箱分数
- 通过销售数据回传持续校准，季度精度递增

### 1.3 成功标准

| 指标 | 第一季目标 | 第二季目标 | 第三季目标 |
|------|-----------|-----------|-----------|
| 样本外Spearman相关系数 | ≥0.55 | ≥0.65 | ≥0.70 |
| S款识别准确率 | ≥60% | ≥75% | ≥85% |
| 风险款识别准确率 | ≥70% | ≥80% | ≥90% |
| 盲测组有效性 | 完成基线 | 新权重不劣化 | 优于旧权重 |

### 1.4 不做什么（边界声明）

- 不预测绝对销量（只预测相对排名与分级）
- 不做配饰类评估
- 不替代人工最终决策（AI提供参考，人保留决策权）
- 不自动学习全新趋势（历史数据无覆盖的新风格由人工判断）
- 不预测营销变量外生影响（主播策略/算法改版/竞品冲击）

---

## 2. 系统架构

### 2.1 四层流水线总览

```
[用户上传：图片+FAB]
        ↓
┌─────────────────────────────┐
│  ① 特征提取层               │
│  VLM输出10个结构化特征向量   │
│  （BARS量表1-10分）          │
└─────────────────────────────┘
        ↓ 特征向量
┌─────────────────────────────┐
│  ② 异构评审层（3引擎并行）    │
│  · 人设投票（20-30人×2-3模型）│
│  · 双渠道评分（规则引擎）     │
│  · 价格价值评分（数学模型）   │
└─────────────────────────────┘
        ↓ 5个评分
┌─────────────────────────────┐
│  ③ 校准层                   │
│  稀疏加权回归聚合             │
│  + 营销控制变量               │
│  + 品类归一                   │
└─────────────────────────────┘
        ↓ 最终预测分
┌─────────────────────────────┐
│  ④ 报告层                   │
│  S/A+/A/P分级                │
│  + 双渠道建议                 │
│  + 扣分项归因                 │
│  + 改款方向                   │
│  + 置信度                     │
└─────────────────────────────┘
        ↓
[输出报告]

销售季结束后 → 真实数据回传 → 校准层更新 → 下一季使用新权重
```

### 2.2 v1→v2 核心转变

| 维度 | v1做法 | v2做法 | 解决的问题 |
|------|--------|--------|-----------|
| VLM角色 | 直接评判"好不好" | 输出结构化特征向量 | S04误判（VLM审美偏差） |
| 评审方式 | 40人设纯投票 | 3个异构引擎并行 | 同源LLM错误相关 |
| 价格判断 | 绝对价格vs手写预算 | 感知价值vs价格百分位 | S06误判（天鹅绒羽绒服） |
| 校准方式 | 手写5条规则 | 稀疏回归学习权重 | v2.0过拟合 |
| 输出形态 | 单一分数+排名 | S/A+/A/P分级+改款建议 | 对齐内审决策框架 |
| 回测机制 | 10款全量验证 | 30款训练+10款封存盲测 | 样本外泛化验证 |

### 2.3 与现有S/A/P框架的映射

| 分级 | 比例预期 | 核心判断维度 | AI输出 |
|------|---------|-------------|--------|
| S（主推款）| ~12% (11/90) | 自然销售力+直播带货力双强 | 双高分+反对率低+价值匹配 |
| A+（准主推）| ~16% (14/90) | 单维度强+另一维度接近 | 单高分+短板可改款升级 |
| A（辅助款）| ~28% (25/90) | 单维度强或综合中位 | 明确主推渠道+改款方向 |
| P（设计款）| ~44% (40-50/90) | 品牌调性贡献+设计独特 | 调性高分+不追求销量 |
| 风险款 | 内审淘汰 | 双渠道均弱+反对率高 | 标红预警，建议大改或淘汰 |

---

## 3. 特征提取层详细设计

### 3.1 10个结构化特征

VLM不直接评判"好不好"，而是用BARS（行为锚定评分量表）输出1-10分。每个分数档有具体的视觉行为锚定描述，VLM执行的是"匹配"任务而非"打分"任务。

#### 物理类特征（客观可测量，校准频率低）

**F01 廓形度 Silhouette**
- 定义：宽松/修身/廓形的程度
- BARS锚定：
  - 1-2：超紧贴身，运动压缩衣感
  - 3-4：修身，略有余量
  - 5-6：常规版型，无明显宽松/紧身
  - 7-8：明显宽松，oversize
  - 9-10：超宽廓形，灯笼/斗篷感

**F02 利落感 Clean Look**
- 定义：视觉整洁度，堆积/褶皱/垂坠/零碎细节多=低分
- BARS锚定：
  - 1-2：大量堆积/垂坠/零碎装饰，视觉混乱
  - 3-4：明显褶皱/oversize/多口袋堆积
  - 5-6：常规版型，无明显利落/不利落特征
  - 7-8：线条干净，少量装饰，版型规整
  - 9-10：极简结构，无多余细节，强线条感
- 解决问题：v1 S04误判根因（oversize工装裤被VLM判为"高级"）

**F03 色彩风险 Color Risk**
- 定义：高饱和/撞色/荧光=高风险；基础色=低风险
- BARS锚定：
  - 1-2：基础中性色（黑/灰/藏青/卡其）
  - 3-4：低饱和度彩色（莫兰迪/大地色）
  - 5-6：中等饱和度单色或简单撞色
  - 7-8：高饱和度撞色/亮色
  - 9-10：荧光/大面积撞色/极强视觉冲击

**F04 功能可见性 Function Visibility**
- 定义：户外功能（防水/防风/防晒/反光/口袋）的视觉显性程度
- BARS锚定：
  - 1-2：无任何功能视觉元素
  - 3-4：隐约功能感（如接缝压胶细节）
  - 5-6：常规户外功能可见（口袋/拉链/帽檐）
  - 7-8：明显功能设计（防水条/反光条/多口袋系统）
  - 9-10：硬核功能外显（全身防水条/大量功能附件/战术感）
- 项目定位：核心特征（品牌方向"户外功能兼顾日常"）

#### 感知类特征（主观但可校准，季度校准）

**F05 上镜感 Photogenic**
- 定义：镜头表现力，直播/短视频画面里是否吸睛、有记忆点
- BARS锚定：
  - 1-2：画面平淡，无视觉记忆点
  - 3-4：略有特色但容易被忽略
  - 5-6：常规画面表现
  - 7-8：有明显视觉记忆点（色彩/廓形/细节）
  - 9-10：极强镜头冲击力，一眼记住
- 用途：直播带货评分核心输入

**F06 实穿度 Wearability**
- 定义：日常穿着场景覆盖度（上学/户外/日常多场景适配）
- BARS锚定：
  - 1-2：极窄场景（仅特定户外活动）
  - 3-4：场景受限（户外可穿/日常勉强）
  - 5-6：常规场景覆盖
  - 7-8：多场景适配（上学/户外/日常均可）
  - 9-10：全场景百搭
- 用途：自然流量评分核心输入

**F07 搭配度 Pairing**
- 定义：与现有衣橱搭配难度，独立性强=高分，挑搭配=低分
- BARS锚定：
  - 1-2：极挑搭配，需特定下装/内搭
  - 3-4：搭配受限
  - 5-6：常规搭配度
  - 7-8：易搭配，多数下装/内搭可用
  - 9-10：百搭，几乎不挑搭配

**F08 面料感知 Fabric Perception**
- 定义：从图片感知的"功能质感+穿着体验预期"
- BARS锚定：
  - 1-2：无功能质感，显廉价
  - 3-4：普通面料感，无明显功能/质感
  - 5-6：常规面料，质感适中
  - 7-8：明显功能质感（防晒涂层/防水面料的科技感）
  - 9-10：高级功能质感（天鹅绒/科技面料/顶级工艺感）
- 校准说明：v1无退货数据，先用行业经验写锚定，1-2季后用真实退货数据校准VLM对面料的判断偏差

#### 品牌类特征（与品牌策略相关，每季人工参与校准）

**F09 品牌调性贡献 Brand Tone**
- 定义：是否符合品牌"6-14岁功能性户外兼顾日常"定位的表达
- BARS锚定：
  - 1-2：完全偏离品牌定位
  - 3-4：方向接近但表达弱
  - 5-6：符合基础调性
  - 7-8：强表达品牌调性（功能+日常平衡）
  - 9-10：品牌调性标杆款
- 用途：P款分级核心

**F10 设计独特性 Uniqueness**
- 定义：市场同类款稀缺度，罕见设计语言/工艺/结构
- BARS锚定：
  - 1-2：完全跟款，市场泛滥
  - 3-4：常见设计语言
  - 5-6：有一定设计特色
  - 7-8：明显独特设计（罕见工艺/结构/细节）
  - 9-10：极罕见设计语言，市场独此一家
- 用途：P款/S款差异化判断

### 3.2 多模型交叉验证

- 特征提取调用2-3个VLM并行（Qwen3-VL-Plus + Qwen3.5-omni + 可选第三模型）
- 每个特征取**中位数**而非平均数（防异常值）
- 输出**分歧度**：三模型在某特征分歧>2分，标记"该特征需人工复核"

### 3.3 特征提取Prompt设计要点

- 不问"好不好"，只问"匹配哪一档"
- 每个特征独立提问，避免特征间相互污染
- 输出格式严格JSON：`{"F01": 7, "F01_confidence": 0.85, "F01_reason": "..."}`
- 置信度<0.6的特征自动触发第二模型复核

---

## 4. 异构评审层详细设计

三个引擎设计哲学完全不同，错误来源互相独立，聚合后偏差互相抵消。

### 4.1 引擎一：人设投票

**目的**：模拟不同类型妈妈的真实购买决策

**人设库设计**（双重决策者模型，30人设）：

童装购买是"妈妈付款+孩子影响"的双重决策，人设卡分三层：

- **妈妈决策者层**（付款人/最终决策）：
  - 妈妈年龄（28-45）
  - 城市级别（一线/新一线/二线/三四线）
  - 家庭月收入（影响价格敏感度）
  - 购买频次（高频复购/季节性/低频）
  - 价值取向（功能实用/颜值优先/品牌认同/性价比）
  - 关注特征：F04功能 + F06实穿 + F07搭配 + F08面料 + F09调性 + 价格

- **孩子影响层**（偏好输入/话语权）：
  - 孩子年龄（6-8/9-11/12-14）
  - 孩子性别（男/女）
  - 颜色偏好（鲜艳/基础/特定色）
  - 图案偏好（卡通IP/几何/运动感/无图案）
  - 款式偏好（运动感/正式/休闲/酷感）
  - 关注特征：F03颜色 + F10图案/独特 + F01廓形（不含穿着体验，AI无法评价）

- **决策模式**（按孩子年龄分档）：
  - 6-8岁：妈妈主导型（70:30），妈妈说了算
  - 9-11岁：共同决策型（50:50），孩子有否决权
  - 12-14岁：孩子主导型（30:70），孩子挑款妈妈付，但妈妈保留否决权（妈妈分<3则直接否决）

**投票逻辑**（双重决策者加权）：
```
Step 1: 妈妈视角评估
  → 穿搭需求(缺不缺) + 款式接受度(版型/颜色/风格) + 价值匹配(用料/功能/价格)
  → 妈妈分 < 3 = 直接否决（即使孩子喜欢也不买）

Step 2: 孩子视角评估（仅图片可见特征）
  → 喜不喜欢(颜色/图案/款式)
  → 孩子分（不评价格/面料/功能/搭配——孩子不在意这些）

Step 3: 决策模式加权
  → 综合分 = 妈妈分 × 妈妈权重 + 孩子分 × 孩子权重
  → 妈妈主导(70:30) / 共同决策(50:50) / 孩子主导(30:70)
```

**多模型混合**：
- 每个人设由2-3个LLM分别扮演（Qwen3-Max + DeepSeek-V4-Pro + 可选Doubao 1.5 Pro）
- 同人设跨模型结果取中位数
- 每人设输出：妈妈购买意愿(1-10) + 孩子喜好分(1-10) + 综合购买意愿 + 妈妈理由 + 孩子理由 + 反对理由

**人设分布权重自动拟合**：

核心原理：不是机器学习，是统计计数+归一化。从订单数据统计真实客户画像分布，作为人设权重。

拟合过程（4步）：
1. 从订单数据提取每笔订单的买家画像标签（孩子年龄/性别/城市/客单价/复购频次）
2. 按画像维度分组计数，算占比
3. 把占比映射到30个人设作为权重
4. 加权聚合投票结果

公式：人设权重 = 该画像在订单数据中的占比 / 该画像对应的人设数

**数据需求**（两种方案）：
- 方案A（最准）：订单级数据（脱敏），每笔订单一行，关联买家画像标签，近12个月全部订单，不含个人身份信息
- 方案B（简化）：聚合统计表，让IT跑SQL按画像维度组合分组，输出每组订单数+GMV，20-50行
- 兜底方案：如果连方案B也拿不到，第一阶段用等权30人设启动，第一季销售数据回传后用"款式画像→销量"反推人设权重（间接拟合）

**输出**：
- 加权票数（基于人设分布权重）
- 反对率（投<4分的人设比例）
- Top 3购买理由 + Top 3反对理由（聚类后输出，区分妈妈理由和孩子理由）

### 4.2 引擎二：双渠道评分（规则引擎，非LLM）

**设计哲学**：不调LLM，用规则公式。与人设投票LLM错误完全独立，天然去相关。

**自然流量评分器**：
```
自然流量分 = 0.40 × F06(实穿度) + 0.30 × F07(搭配度) + 0.30 × (10 - F03)
```
- 关注：实穿度高+搭配度高+色彩风险低 = 妈妈搜索/复购型购买
- 高分含义：自然流量渠道（天猫/淘宝搜索）易走量

**直播带货评分器**：
```
直播带货分 = 0.35 × F05(上镜感) + 0.35 × F04(功能可见性) + 0.30 × F10(设计独特性)
```
- 关注：上镜感高+功能可见性高+设计独特性高 = 主播有料可讲、画面吸睛
- 高分含义：直播渠道（抖音/快手）易转化

**输出**：两个独立分（不是综合分），用于S/A+/A/P矩阵定位

**权重校准**：
- 第一季用行业经验初始权重（如上）
- 第一季后用销售数据（自然vs直播渠道拆分）回归校准
- 权重调整幅度限制：单季不超过20%

### 4.3 引擎三：价格价值评分（数学模型，非LLM）

**核心重构**：从"价格vs品类预算"改为"感知价值vs价格感知百分位"

**感知价值模型**：
```
感知价值期望 = α·F04(功能质感) + β·F08(面料感知) + γ·F10(设计独特) + δ·F09(品牌调性)
            + ε·(F04 × F08)  [交互项：功能×面料的联合感知]

价值匹配度 = 感知价值期望 - 价格感知百分位

价格感知百分位 = 该款价格在品类历史订单分布中的百分位
```

**关键设计点**：
1. 感知价值由4个特征+1个交互项决定，权重α/β/γ/δ/ε从历史数据回归学习
2. "价格的感知百分位"取代"绝对价格超预算"——同样400元，在羽绒品类目里是中位价，在T恤类目里是高价
3. 交互项(F04×F08)处理"功能+面料联合感知"——防晒涂层+凉感面料的组合感知价值 > 单独相加

**示例**：
| 款式 | 价格 | F04 | F08 | F10 | 感知价值 | 价值匹配 | 结论 |
|------|------|-----|-----|-----|---------|---------|------|
| 天鹅绒羽绒服 | ¥800 | 7 | 9 | 8 | 8.2(高) | +0.2 | 价格配得上，不扣分 |
| 普通羽绒服 | ¥400 | 5 | 5 | 4 | 4.8(低) | -0.8 | 虽便宜但价值不配，扣分 |
| 低质冲锋衣 | ¥150 | 3 | 3 | 3 | 3.0(极低) | -0.5 | 便宜但用料差，仍扣分 |

**输出**：价值匹配度 + 价格风险标签（高/中/低）

**品类预算分布**：
- 从品牌历史订单数据拟合（同品类、同时段、扣大促）
- 输入：品类、价格
- 输出：该价格在分布中的百分位

---

## 5. 校准层详细设计

### 5.1 功能

将三个引擎的5个评分 + 10个特征向量 → 稀疏加权回归 → 最终预测分

### 5.2 训练数据标签

- **训练标签**：品类内售罄率百分位（而非绝对销量）
- 例：某款在冲锋衣品类内的售罄率排名为第85百分位 → 标签=0.85
- 归一化处理消除品类间销量差异

### 5.3 回归模型

- 算法：稀疏线性回归（L1正则化/LASSO）
- 特征数限制：≤5个核心特征（防过拟合）
- 训练方式：留一法交叉验证（LOO-CV），适合小样本
- 权重漂移限制：新权重不能偏离上一季±30%

### 5.4 控制变量

训练时必须作为控制变量的混淆因素：
- **营销加成**：该款是否被主推/直播/打折（分离"款式本身好"和"被推了才好卖"）
- **品类归一**：不同品类基线销量差异
- **时间衰减**：越近的季度数据权重越高（半衰期2季）

### 5.5 防过拟合机制

- 训练集/测试集严格切分：30款训练 + 10款封存盲测
- v2.0规则定稿后**不能回头再改**
- 盲测组：每季10-20%的款不参考AI预测、正常操作，事后检验新权重
- 如果新权重在盲测组上比旧权重还差，回滚

---

## 6. 闭环反馈机制

### 6.1 三层学习

**第一层：校准层权重更新（最可靠，核心闭环）**
- 学什么：哪些特征真正预测销量
- 怎么学：LOO-CV稀疏回归，5个以内核心特征权重
- 防坑：时间衰减+营销控制+盲测组验证+权重漂移限制±30%

**第二层：Persona分布权重调整**
- 学什么：哪些人设投票与真实购买行为最相关
- 怎么学：季度回顾Spearman相关性，调权重
- 防坑：单季调整≤20%，所有人设最低权重≥5%（保留多样性）

**第三层：VLM特征提取校准（最慢，半手动）**
- 学什么：VLM对某些特征的系统性偏差
- 怎么学：人工参与，调整BARS锚定描述
- 频率：每季度回顾一次

### 6.2 三个硬边界（不能从数据学的东西）

1. **全新趋势无法预测**：历史数据无覆盖的新风格，保留"新趋势探测器"——VLM/人设发现某款特征模式与历史不同时标记"趋势异常"，人工判断
2. **营销变量外生变化**：主播策略/算法改版/竞品冲击不是款式问题，校准层必须输入营销标签作为控制变量
3. **VLM模型更新打乱**：每次换模型版本，必须用"锚定款"（已知结果的经典款）验证特征提取一致性，偏差超阈值需重新校准

### 6.3 运行节奏

| 时间点 | 动作 |
|--------|------|
| 每季前 | 用上季销售数据更新校准权重+persona分布；回顾VLM特征偏差 |
| 内审前4-6周 | 输入新款式，AI输出S/A+/A/P分级+渠道建议+改款建议 |
| 内审时 | AI结果辅助参考，团队讨论决策，记录最终人工分级 |
| 上新后2-4周 | 用早期数据（加购率/收藏率/7天转化）中期校验，严重不符触发预警 |
| 销售季结束 | 回传完整销售数据，运行误差分析，准备下一季校准 |

---

## 7. S/A/P分级与报告层

### 7.1 决策矩阵

S/A/P不是简单的高分→S、中分→A、低分→P，而是基于双维度矩阵：

```
                    自然销售力 → 强
         ┌────────────────┬────────────────┐
         │                │                │
直播带货 │   S (双强主推)  │  A (直播潜力)   │
力 → 强 │   ~12%         │  ~16% A+       │
         │   深度备货      │  针对性改款     │
         ├────────────────┼────────────────┤
         │  A (基础跑量)   │  P (设计调性)   │
         │  ~28%          │  ~44%          │
         │  控深度         │  小批量试产     │
         └────────────────┴────────────────┘
                   ↑
         品牌调性贡献维度单独判断P款

风险款：双渠道均弱 + 反对率>30% → 不建议主推，大改或淘汰
```

### 7.2 分级标准

| 分级 | 自然分 | 直播分 | 附加条件 | 备货策略 |
|------|--------|--------|---------|---------|
| S | ≥7.5 | ≥7.0 | 反对率≤15% + 价值匹配≥0 | 深度备货，核心资源位 |
| A+ | 单维度≥7.5，另一维度6.0-7.0 | 短板可改款升级 | 选择性备货 |
| A | 单维度≥7.0 | 明确主推渠道 | 控深度 |
| P | 不要求 | 品牌调性≥7.0 + 设计独特≥7.5 | 小批量试产 |
| 风险 | <5.0 | <5.0 或 反对率>30% | 大改或淘汰 |

### 7.3 改款建议生成逻辑

针对A+/A款的改款建议是工具核心差异化价值：

1. 识别扣分项：哪个特征分最低？
2. 匹配改款方向库：
   - 利落感低(F02<5) → 收窄廓形/减少堆积细节
   - 色彩风险高(F03>7) → 增加基础色选项
   - 功能可见性低(F04<5) → 强化功能可视化设计
   - 实穿度低(F06<5) → 优化版型适配多场景
   - 面料感知低(F08<5) → 调整面料/工艺
   - 价值匹配为负 → 调整价格或强化用料感知
3. 输出具体建议，而非"优化版型"空话

### 7.4 报告输出格式

每款报告包含：
1. **分级标签**：S/A+/A/P/风险
2. **总分与置信度**：0-100分 + 置信度（基于特征分歧度+人设一致性）
3. **双渠道建议**：自然分/直播分 + 主推渠道建议
4. **关键扣分项**：Top 3低分特征 + 具体数值
5. **改款方向**：针对扣分项的具体建议（A+/A款）
6. **消费者洞察**：Top 3购买理由 + Top 3反对理由
7. **价值匹配**：价格风险标签
8. **人设分歧度**：哪些人设群体分歧大（潜在客群定位问题）

---

## 8. LLM选型与API接入控制

### 8.1 模型选型（基于2025-2026评测调研）

| 角色 | 模型 | 平台 | 成本 | 选型理由 |
|------|------|------|------|---------|
| 视觉理解主力 | Qwen3.5-omni / Qwen3-VL-Plus | 阿里云百炼 | ¥0 | 原生多模态，多基准接近GPT-4o，中文场景原生优化 |
| 人设扮演第二 | Qwen3-Max / Doubao 1.5 Pro | 百炼/火山 | ¥0 | 纯文本推理强，Doubao 1.5 Pro多模态评测85分超GPT-4o |
| 人设扮演第三 | DeepSeek-V4-Pro | 百炼上架 | ¥0 | 完全不同厂商，错误独立性最佳 |

**关键技术决策**：
- 不用GPT-4o（付费+国内访问不便+Doubao已超越）
- 全部使用阿里云百炼免费层（2026年8月全线免费）
- Qwen3+DeepSeek虽同平台但不同厂商训练，错误独立性足够

### 8.2 API接入控制方案

**API Key管理**：
- 所有API Key通过环境变量注入，不硬编码
- 统一API客户端封装，支持多模型切换
- QPM（每分钟调用次数）限流控制，避免触发免费层限制

**调用策略**：
- 特征提取：串行调用（1次/款），QPM控制60以内
- 人设投票：并行调用（2-3模型×20-30人设），分批控制
- 失败重试：指数退避，最多3次
- 成本监控：记录每次调用的Token消耗

**速率限制应对**：
- 百炼免费层QPM限制：60次/分钟
- 90款×(1次特征提取 + 60次人设投票) ≈ 5500次调用
- 串行执行约2-3小时，可接受
- 可选：异步并发+队列管理，缩短至30-60分钟

### 8.3 成本估算

| 项目 | 成本 |
|------|------|
| 特征提取 | ¥0 |
| 人设投票 | ¥0 |
| 双渠道评分 | ¥0（规则引擎） |
| 价格价值评分 | ¥0（数学模型） |
| 单款总成本 | ≈ ¥0 |
| 90款/季 | ≈ ¥0（仅消耗免费额度） |

---

## 9. 页面设计与自动化流程

### 9.1 用户操作流程（高度自动化）

```
用户操作：
1. 上传款式图片（1-4张/款）
2. 填写款式FAB说明（可选，AI可从图片自动提取）
3. 填写价格
4. 选择品类（冲锋衣/速干T恤/羽绒服/裤装等）
5. 点击"开始评审"

AI自动完成：
1. 图片预处理（裁剪/尺寸标准化）
2. FAB解析（如用户填写）或VLM自动提取款式描述
3. 特征提取（10个特征，多模型交叉验证）
4. 人设投票（20-30人设×2-3模型）
5. 双渠道评分（规则引擎）
6. 价格价值评分（数学模型）
7. 校准层聚合
8. 报告生成

输出：
- 单款报告（分级+建议+洞察）
- 批量排名报告（90款S/A+/A/P分布）
- 导出Excel/PDF
```

### 9.2 页面设计原则

- **简洁干净**：无多余装饰，聚焦功能
- **单页操作**：上传→配置→结果，一页完成
- **进度可见**：评审过程显示进度条+当前步骤
- **结果直观**：报告用卡片式布局，分级用颜色标签
- **可导出**：支持Excel/PDF导出，便于内审会议使用

### 9.3 页面结构

**页面1：上传与配置**
- 款式列表（表格形式，支持批量上传）
- 每行：图片缩略图 + 款号 + 价格 + 品类 + FAB（可选）
- 底部："开始评审"按钮

**页面2：评审进度**
- 总进度条（X/90款完成）
- 当前款状态：特征提取中/人设投票中/报告生成中
- 实时日志（可折叠）
- 失败款重试按钮

**页面3：结果报告**
- 概览卡片：S/A+/A/P/风险 各档数量+占比
- 款式列表（可排序/筛选）：款号+图片+分级+总分+双渠道分+主推渠道
- 点击单款 → 展开详情：扣分项+改款建议+消费者洞察+价值匹配

**页面4：回测校准（内部使用）**
- 上传真实销售数据
- 运行Spearman计算
- 显示预测vs实际散点图
- 权重更新建议（需人工确认）

---

## 10. 数据需求清单

### 10.1 品牌需提供的数据（分优先级）

**🔴 必须（无此数据无法启动）**：

1. **历史款式销量数据**（30-50款，覆盖2-3季）
   - 款号
   - 品类
   - 实际成交均价（非标价）
   - 总销量
   - 首批备货量
   - 售罄率
   - 统计周期（起止日期）
   - S/A/P人工分级标签

2. **品类价格分布**（用于价格感知百分位）
   - 各品类近12个月订单价格分布
   - 分位数：P25/P50/P75/P90

**🟡 加分（显著提升精度）**：

3. **渠道拆分销量**
   - 自然流量渠道销量
   - 直播渠道销量
   - 其他渠道销量

4. **早期指标数据**（用于中期校验）
   - 上新7天加购率
   - 收藏率
   - 转化率

**🟢 有则更好（用于persona分布拟合）**：

5. **买家画像数据**（脱敏，不含个人身份信息）

   方案A（最准，推荐）：订单级数据
   - 每笔订单一行，近12个月全部订单
   - 字段：订单ID（脱敏） + SKU/款号 + 品类 + 成交价 + 购买时间 + 收货城市 + 买家画像标签（孩子年龄/性别 + 客单价区间 + 复购频次）
   - 用途：精确拟合人设分布权重

   方案B（简化，够用）：聚合统计表
   - 让IT跑SQL，按画像维度组合分组
   - 输出：画像组合 + 订单数 + GMV + 平均客单价，20-50行
   - 用途：拟合人设分布权重（精度略低但够用）

   兜底方案：如果连方案B也拿不到，第一阶段用等权30人设启动，第一季销售数据回传后间接拟合

6. **营销标签**
   - 每款是否主推
   - 是否直播重点款
   - 折扣力度

### 10.2 数据格式要求

- 销量数据：Excel/CSV，一行一款
- 图片：每款1-4张，命名`款号_序号.jpg`（如S01_1.jpg）
- 文件夹结构：
  ```
  /data
    /styles.xlsx      # 款式数据
    /sales.xlsx       # 销量数据（回测用）
    /images/          # 款式图片
      S01_1.jpg
      S01_2.jpg
      ...
  ```

---

## 11. 技术栈选型

### 11.1 后端

- **语言**：Python 3.11+
- **Web框架**：FastAPI（异步支持，适合多LLM并发调用）
- **LLM客户端**：dashscope（百炼官方SDK，兼容OpenAI格式）
- **数据处理**：pandas + numpy
- **机器学习**：scikit-learn（LASSO回归）
- **统计计算**：scipy.stats（Spearman相关）

### 11.2 前端

- **框架**：React + Vite
- **UI库**：Ant Design（简洁干净的组件库）
- **图表**：Recharts（散点图/柱状图）
- **状态管理**：Zustand（轻量）

### 11.3 部署

- **阶段A（自己跑）**：本地运行，FastAPI + React dev server
- **阶段B（品牌方用）**：Docker容器化部署，可上云

### 11.4 存储

- **图片**：本地文件系统（MVP阶段）
- **数据**：SQLite（MVP阶段，单文件易管理）
- **配置**：YAML文件（人设库/特征定义/BARS量表）

---

## 12. 开发阶段划分

### 阶段一：核心验证（2-3周）

**目标**：用历史10款数据验证方法论，Spearman从v1的0.55提升到0.65+

**范围**：
- 特征提取层（10特征+BARS+多模型交叉验证）
- 人设投票引擎（20人设+2模型混合）
- 双渠道评分（规则引擎）
- 价格价值评分（数学模型，初始权重）
- 简化校准（先用v1的10款做LOO-CV）
- 命令行版回测脚本
- 输出：Spearman系数+预测vs实际散点图

**交付物**：
- `feature_extraction.py` 特征提取模块
- `persona_voting.py` 人设投票模块
- `channel_scoring.py` 双渠道评分模块
- `price_value.py` 价格价值评分模块
- `backtest.py` 回测脚本
- 回测报告

### 阶段二：产品化（2-3周）

**目标**：做成品页面工具，支持品牌方上传款式→自动评审→报告输出

**范围**：
- Web界面（上传+配置+进度+报告4页面）
- API接入控制（百炼SDK封装+限流+重试）
- 报告生成（卡片式布局+导出Excel/PDF）
- 批量评审（90款队列管理）
- 配置文件化（人设库/特征定义/BARS可编辑）

**交付物**：
- 完整Web应用
- Docker部署配置
- 用户操作手册

### 阶段三：闭环与校准（1-2周，第一季销售数据回传后）

**目标**：实现闭环反馈机制

**范围**：
- 销售数据回传接口
- LOO-CV校准层训练
- Persona分布权重调整
- 误差分析报告
- 权重漂移控制+盲测组验证

**交付物**：
- `calibration.py` 校准模块
- 回测校准页面
- 季度校准报告

---

## 13. 风险与边界

### 13.1 技术风险

| 风险 | 概率 | 影响 | 缓解措施 |
|------|------|------|---------|
| VLM对oversize/莫兰迪色系统偏差 | 高 | S04类误判 | BARS锚定+季度校准+交互项 |
| 小样本过拟合 | 高 | 校准层失真 | LOO-CV+5特征限制+权重漂移限制 |
| 百炼免费层QPM限制 | 中 | 评审耗时增加 | 队列管理+异步并发+分批处理 |
| 模型版本更新打乱特征 | 中 | 校准失效 | 锚定款验证+版本锁定 |
| 新趋势无法预测 | 高 | 预测失效 | 新趋势探测器+人工介入 |

### 13.2 业务边界

- AI结果为辅助参考，不替代人工最终决策
- 不预测绝对销量，只预测相对排名与分级
- 不覆盖全新趋势风格
- 不预测营销外生变量影响
- 需每季度维护，维护成本递减（前2-3季频繁，之后微调）

### 13.3 成功边界

本工具的成功不在于"AI比人准"，而在于：
1. 提供可量化的辅助决策依据
2. 暴露人脑忽略的盲区（如S04类VLM偏差、S06类价格错配）
3. 输出可解释的改款方向
4. 季度精度递增，建立长期信任

---

## 14. 验收清单

- [ ] 特征提取层：10个特征BARS输出，多模型交叉验证
- [ ] 人设投票：20+人设，2+模型混合，三步漏斗心理模型
- [ ] 双渠道评分：规则引擎，自然分+直播分独立输出
- [ ] 价格价值评分：感知价值模型，交互项，品类百分位
- [ ] 校准层：LOO-CV，5特征限制，权重漂移控制
- [ ] S/A+/A/P分级：双维度矩阵，风险款预警
- [ ] 改款建议：针对扣分项的具体方向
- [ ] 报告层：卡片式布局，Excel/PDF导出
- [ ] 回测：Spearman≥0.55（第一季目标）
- [ ] API接入：百炼全家桶，环境变量管理，限流控制
- [ ] 页面：上传→进度→报告，简洁干净
- [ ] 闭环：销售数据回传→校准更新

---

## 附录A：v1问题清单与v2解法对照

| v1问题 | 根因 | v2解法 | 章节 |
|--------|------|--------|------|
| S04误判（莫兰迪工装裤高估） | VLM审美系统偏差 | BARS结构化特征提取+利落感特征 | §3 |
| S06误判（价格错配） | 绝对价格vs手写预算 | 感知价值模型+品类百分位 | §4.3 |
| 40人设有效样本<<40 | 同源LLM错误相关 | 3异构引擎+多模型混合 | §4 |
| v2.0过拟合 | 测试集调规则 | LOO-CV+权重漂移限制+盲测组 | §5 |
| 0.47统计不显著 | n=10样本太小 | 30款训练+10款封存 | §5 |
| 缺乏S/A/P框架对齐 | 单一分数排序 | 双维度矩阵+5档分级 | §7 |

## 附录B：BARS量表完整定义

（见§3.1，10个特征的完整BARS锚定描述）

## 附录C：人设库初始设计

（待阶段一开发时细化，基于品牌订单数据分布拟合）

---

## 第6章 通用消费者决策三层模型

> 本章定义跨品类通用的消费者决策三层模型，对应CORE引擎的三大异构引擎，是 fashion-hit-engine 区别于单一LLM打分工具的核心理论基础。

### 6.1 决策模型总览

消费者服装购买决策不是单一"喜欢/不喜欢"，而是三个相对独立子系统的加权结果，分别对应行为经济学中的 **System1（直觉系统）、System2（理性系统）、Signaling（身份信号系统）**：

```
          ┌─────────────────────────────────────────────┐
          │           消费者购买决策 = f(S1,S2,Sign)    │
          └─────────────────────────────────────────────┘
                           ↓              ↓              ↓
                    System1视觉     System2价值      Signaling身份
                      观感(S1)        评估(S2)        表达(Sign)
                           ↓              ↓              ↓
                      引擎二          引擎三          引擎一
                 BARS量表10特征   感知价值模型     30人设投票
                 （VLM结构化）   （价格百分位）   （轴线加权）
```

三层决策的错误来源完全独立，因此三个异构引擎的集成有天然的去偏差效果。

### 6.2 System1 · 视觉观感（对应引擎二）

**本质**：0.5秒内完成的直觉判断，由大脑视觉皮层快速处理，消费者自己往往说不清理由。

**输入**：图片视觉特征（廓形、色彩、利落度、上镜感、功能可见性等）

**处理方式**：VLM + BARS量表结构化输出10个特征分值（1-10分），不做综合评判。

**关键特征**：
- F01 廓形度 Silhouette
- F02 利落感 Clean Look
- F03 色彩风险 Color Risk
- F04 功能可见性 Function Visibility
- F05 上镜感 Photogenic

**校准方式**：3Loop Loop1 — 统计每个特征f的Spearman ρ_f，做偏置校准，clamp [0.7, 1.3]。

### 6.3 System2 · 价值评估（对应引擎三）

**本质**：需要几秒~几分钟的理性计算，权衡"值不值这个价"。

**输入**：System1输出的部分特征（功能、面料、独特性、品牌调性）+ 售价 + 品类历史价格分布

**处理方式**：数学模型（规则引擎，非LLM）

```
感知价值期望 PV = α·F04 + β·F08 + γ·F10 + δ·F09 + ε·(F04×F08)
价格感知百分位 PP = rank(该款售价) / 该品类历史订单总数
价值匹配度 VM = PV - PP
价格风险 = |VM| 的分档
```

**交互项说明**：F04（功能可见性）× F08（面料感知）的联合感知价值 > 两者单独相加，是"功能+面料"组合溢价的来源。

**校准方式**：3Loop Loop3 的一部分 — α/β/γ/δ/ε 权重随销售数据微调。

### 6.4 Signaling · 身份表达（对应引擎一）

**本质**：服装作为社交信号，"这件衣服让我看起来像什么人"。

**输入**：10特征向量 + FAB描述 + 售价 + 人设卡（身份三轴线定义）

**处理方式**：多LLM混合扮演30人设，输出购买意愿(1-10) + 支持理由 + 反对理由，按人设分布权重加权聚合。

**童装特殊扩展**：童装的双层决策结构（妈妈决策者层 + 孩子影响层）是 Signaling 层的**可选扩展层**，不是通用核心。女装/男装/快消等品类使用 single_layer 模式，不加载孩子否决层。

**校准方式**：3Loop Loop2 — Lasso稀疏回归从真实销量反推30人设权重分布。

---

## 第7章 身份三轴线人设定义

> 本章定义品牌适配包中 30人设的通用生成公式：**身份三轴线 = 轴线A场景 × 轴线B审美偏好 × 轴线C价格敏感度**，30 = 5 × 3 × 2。

### 7.1 三轴线定义

| 轴线 | 维度 | 取值数 | 说明 | 典型取值（示例） |
|------|------|--------|------|----------------|
| **轴线A · 核心场景** | 消费者穿这件衣服的**首要使用场景** | 5档 | 场景决定了功能需求和实穿度预期 | 通勤职场 / 周末休闲 / 户外运动 / 社交聚会 / 居家日常 |
| **轴线B · 审美偏好** | 消费者偏好的**视觉风格轴线** | 3档 | 审美偏好决定了对廓形、利落度、色彩风险的容忍度 | 经典保守型 / 平衡主流型 / 先锋前卫型 |
| **轴线C · 价格敏感度** | 消费者对价格的**感知模式** | 2档 | 价格敏感度决定了价值匹配VM的权重和阈值 | 性价比敏感型 / 品质溢价接受型 |

### 7.2 30人设生成公式

```
人设总数 = |轴线A| × |轴线B| × |轴线C| = 5 × 3 × 2 = 30
```

每个人设由三轴线唯一确定，形成正交完备的消费者画像空间：

```
Persona(i_A, i_B, i_C) = {
  axis_A: SceneTypes[i_A],       # i_A ∈ {0,1,2,3,4}
  axis_B: AestheticTypes[i_B],   # i_B ∈ {0,1,2}
  axis_C: PriceTypes[i_C],       # i_C ∈ {0,1}
  weight: w(i_A,i_B,i_C),        # 品牌真实人群分布权重，初始=1/30
 关注特征: 根据轴线组合自动映射（如户外场景→F04权重高）
 购买阈值: 分档（如价格敏感型→VM≥-0.2才买）
}
```

### 7.3 童装扩展层（非核心）

童装品牌适配包（如 tongzhuang-outdoor）在三轴线30人设之外，额外叠加**孩子否决层**作为扩展决策维度：

```
童装综合分 = 妈妈决策者分 × w_mom + 孩子偏好分 × w_child
w_mom : w_child 按孩子年龄分档：
  6-8岁  → 70:30（妈妈主导）
  9-11岁 → 50:50（共同决策，孩子有否决权）
  12-14岁→ 30:70（孩子主导，妈妈保留否决权，妈妈分<3直接否决）
```

**注意**：孩子否决层是 `profile.yaml` 中 `decision_structure: double_layer` 的特有配置。女装/男装等品类使用 `single_layer`，不加载此扩展层。

### 7.4 人设权重的品牌特异性

30人设的**定义**通用（三轴线正交空间），但**权重分布**高度品牌特异性：
- 高端户外品牌：轴线A=户外运动占比高，轴线C=品质溢价接受型占比高
- 大众快消品牌：轴线A=周末休闲/通勤占比高，轴线C=性价比敏感型占比高
- 设计师潮牌：轴线B=先锋前卫型占比高，轴线A=社交聚会占比高

初始权重可等权（1/30）启动，第一季销售数据回传后通过 3Loop Loop2 Lasso稀疏拟合自动收敛到品牌真实分布。

---

## 第8章 核心优化内核3Loop数学形式 + 保护机制

> 本章给出 3Loop 优化内核的精确数学形式和保护机制，是"Spearman每轮单调提升、永不越调越差"的实现保障。

### 8.1 Loop1 · 校准VLM特征偏置

**目的**：修正VLM对某些特征的系统性高估/低估（如VLM天生偏爱oversize廓形，但实际市场不买账）。

**步骤**：

1. 对每个VLM特征 f ∈ {F01, F02, ..., F10}，计算Spearman秩相关系数：
   ```
   ρ_f = Spearman(x_f, y)
   ```
   其中 x_f = 该特征在全部款式上的分值向量，y = 真实销量排名向量。

2. 计算平均相关系数：
   ```
   ρ_mean = (1/10) Σ ρ_f
   ```

3. 计算每个特征的偏置校准系数：
   ```
   bias_f = ρ_f / ρ_mean
   ```

4. **区间夹紧（Clamp）**：防止单个特征被过度放大/压缩：
   ```
   bias_f = clamp(bias_f, 0.7, 1.3)
   ```

5. 应用校准：后续所有引擎的特征向量做线性缩放：
   ```
   x_f' = x_f × bias_f
   ```

### 8.2 Loop2 · Lasso稀疏拟合品牌真实人群分布

**目的**：从真实销量反推30人设的真实权重分布，稀疏性保证只有真正有区分力的人设被调高。

**数学形式**：

求解L1正则化线性回归：
```
min_w  Σ_i (y_i - Σ_k w_k · vote_k(x_i))² + λ · Σ_k |w_k|
s.t.   Σ_k w_k = 1
       w_k ≥ w_min = 1/(2×30)  # 每人设最少权重≈1.67%，保留多样性
```

其中：
- y_i = 第i款真实销量排名（0-1归一化）
- vote_k(x_i) = 第k个人设对第i款的购买意愿分（1-10 → 0-1归一化）
- w_k = 第k个人设的权重（待求解）
- λ = Lasso正则强度，通过留一法交叉验证（LOO-CV）选择最优值

**稀疏效果**：λ越大，越多w_k被压到接近w_min下限，只保留真正有预测力的人设获高权重。

### 8.3 Loop3 · 调三大引擎+双渠道相对权重

**目的**：根据三大引擎各自的Spearman表现，动态调整集成权重，保证每个引擎有最低话语权。

**步骤**：

1. 分别计算三大引擎独立的Spearman：
   ```
   ρ_sign   = Spearman(引擎一·身份表达集成分, y)
   ρ_s1     = Spearman(引擎二·视觉观感集成分, y)
   ρ_s2     = Spearman(引擎三·价值评估集成分, y)
   ```

2. 计算原始权重（保证每个引擎最少5%话语权）：
   ```
   w'_sign = max(0.05, ρ_sign + 0.3)
   w'_s1   = max(0.05, ρ_s1   + 0.3)
   w'_s2   = max(0.05, ρ_s2   + 0.3)
   ```

3. 归一化：
   ```
   w_sign = w'_sign / (w'_sign + w'_s1 + w'_s2)
   w_s1   = w'_s1   / (w'_sign + w'_s1 + w'_s2)
   w_s2   = w'_s2   / (w'_sign + w'_s1 + w'_s2)
   ```

4. 双渠道权重同理，按自然分/直播分各自的Spearman计算。

5. 最终预测分：
   ```
   ŷ = w_sign · y_sign + w_s1 · y_s1 + w_s2 · y_s2
   ```

### 8.4 保护机制：Spearman单调不减承诺

**核心硬约束**：每个Loop更新后必须满足以下条件，否则直接回滚到更新前的状态。

```
定义：
  ρ_old = 更新前的Spearman(ŷ_old, y)
  ρ_new = 更新后的Spearman(ŷ_new, y)
  MIN_IMPROVEMENT = 0.005（可在scoring_weights.yaml中配置）

更新规则：
  IF ρ_new ≥ ρ_old + MIN_IMPROVEMENT:
      接受更新，保存新参数
  ELSE:
      拒绝更新，返回旧参数，记录本次尝试（供人工排查）
```

**三层保护的执行顺序**：
1. Loop1执行 → 保护检查 → 通过才保存
2. 在Loop1结果基础上执行Loop2 → 保护检查 → 通过才保存
3. 在Loop1+Loop2结果基础上执行Loop3 → 保护检查 → 通过才保存

最终结果保证 ρ_final ≥ ρ_initial，永不越调越差。

### 8.5 MIN_IMPROVEMENT的意义

设置极小的正阈值（而非≥0）是为了避免数值精度和随机波动导致的"假提升"，确保每次更新都是统计上有意义的真实提升。

---

## 第9章 残差分离机制

> 本章定义残差分离器的数学形式和执行逻辑，是防止3Loop学到伪相关的核心保护。

### 9.1 残差定义

对每一款 i，定义预测残差：
```
ε_i = y_i - ŷ_i
```
其中 y_i = 真实销量排名（0-1归一化），ŷ_i = 3Loop校准后的预测分。

对全部款式的残差做正态分布拟合，得到均值μ和标准差σ：
```
μ = (1/N) Σ ε_i
σ² = (1/(N-1)) Σ (ε_i - μ)²
```

### 9.2 残差分档规则

```
IF ε_i > μ + 2σ:
    标记为「超预期款」—— 卖得显著比AI预测好
    → 运营复盘方向：KOL带火？热搜同款？竞品意外缺货？主播偏爱？
ELIF ε_i < μ - 2σ:
    标记为「不及预期款」—— 卖得显著比AI预测差
    → 运营复盘方向：质量事故？差评扩散？竞品大促狙击？尺码问题？
ELSE:
    残差在正常范围内，属于模型可解释的正常波动
```

**统计直觉**：正态分布下，±2σ之外的概率约为4.55%，因此每批只会有极个别款式被标记，保证运营复盘聚焦真正的异常。

### 9.3 残差铁则：绝对不喂回3Loop

> **残差款是运营复盘素材，不是校准训练数据。** 违反此铁则会导致模型学到伪相关，Spearman长期倒退。

**伪相关案例**：
- 某季度KOL碰巧穿了一件亮黄色冲锋衣 → 该款爆卖 → ε >> μ+2σ
- 如果把此残差喂回Loop1校准 → F03色彩风险的ρ_f被异常拉高 → 模型"学到"亮黄色=爆款
- 下季度亮黄色款式被高估，但没有KOL加持 → 预测彻底失真 → Spearman暴跌

**正确处理方式**：
1. 残差款（±2σ之外）在3Loop训练前**被单独剔除**，不参与Loop1/2/3的任何计算
2. 残差款ID列表写入校准报告，供运营团队做定性复盘
3. 运营若发现残差背后有可复制的真实规律（如"亮黄色+KOL组合"可作为新营销策略），由人工决策是否在下一版本通过**品牌适配包YAML手动调整**，而非让模型自动学习

### 9.4 残差款识别的运营价值

残差分离器的输出本身就是高价值运营洞察：
- **超预期款**：可能包含未被模型捕捉的新趋势信号，运营可挖掘背后的可复制因素
- **不及预期款**：可能暴露供应链/质检/竞品等模型看不到的系统性风险，提前预警

---

## 品牌适配包YAML规格

> 本章定义 `brand_profiles/<brand_id>/` 目录下5个YAML文件的完整规格。每个新品类/新品牌 = 复制 `_template/` 目录，修改这5个YAML，Python代码一行不动。

### YAML1 · profile.yaml — 决策结构定义

```yaml
# profile.yaml
brand_id: nvzhuang-tongqin         # 唯一ID，字母数字下划线
brand_name: 女装通勤品牌X           # 展示名
decision_structure: single_layer   # single_layer / double_layer
                                    # single_layer: 通用单决策者
                                    # double_layer: 童装双层（妈妈+孩子）

# single_layer模式下不需要以下字段
# double_layer模式下必填：
# child_age_groups: [6-8, 9-11, 12-14]
# mom_child_weights: {6-8: [0.7, 0.3], 9-11: [0.5, 0.5], 12-14: [0.3, 0.7]}
# mom_veto_threshold: 3.0            # 妈妈分低于此直接否决
```

### YAML2 · features_bars.yaml — 10特征BARS锚定量表

```yaml
# features_bars.yaml
features:
  F01_silhouette:
    name: 廓形度
    category: physical              # physical / perceptual / brand
    bars:                           # 1-10分每2分一档的锚定描述
      1-2: 超紧贴身，运动压缩衣感
      3-4: 修身，略有余量
      5-6: 常规版型，无明显宽松/紧身
      7-8: 明显宽松，oversize
      9-10: 超宽廓形，灯笼/斗篷感

  F02_clean_look:
    name: 利落感
    category: physical
    bars:
      1-2: 大量堆积/垂坠/零碎装饰，视觉混乱
      3-4: 明显褶皱/oversize/多口袋堆积
      5-6: 常规版型，无明显利落/不利落特征
      7-8: 线条干净，少量装饰，版型规整
      9-10: 极简结构，无多余细节，强线条感
    # ... F03~F10 同理，共10个特征
```

### YAML3 · personas.yaml — 30人设 + 身份三轴线定义

```yaml
# personas.yaml
axes_definition:
  axis_a_scene:                     # 轴线A 核心场景（5档）
    - id: commute
      name: 通勤职场
      focus_features: [F02_clean_look, F06_wearability, F08_fabric]
    - id: weekend
      name: 周末休闲
      focus_features: [F06_wearability, F07_pairing, F01_silhouette]
    - id: outdoor
      name: 户外运动
      focus_features: [F04_function_visibility, F08_fabric, F06_wearability]
    - id: social
      name: 社交聚会
      focus_features: [F05_photogenic, F10_uniqueness, F09_brand_tone]
    - id: home
      name: 居家日常
      focus_features: [F06_wearability, F08_fabric, F01_silhouette]

  axis_b_aesthetic:                 # 轴线B 审美偏好（3档）
    - id: classic
      name: 经典保守型
      color_risk_tolerance: low     # 对F03容忍度
    - id: balanced
      name: 平衡主流型
      color_risk_tolerance: medium
    - id: avant_garde
      name: 先锋前卫型
      color_risk_tolerance: high

  axis_c_price:                     # 轴线C 价格敏感度（2档）
    - id: value_sensitive
      name: 性价比敏感型
      vm_threshold: -0.2            # 价值匹配VM低于此不买
    - id: quality_premium
      name: 品质溢价接受型
      vm_threshold: -0.8

# 30人设 = 5 × 3 × 2 自动生成，此处可覆盖特定人设的权重初始值
persona_overrides:
  # 例：如果知道品牌核心客群是通勤+平衡主流+性价比敏感，可手动调高初始权重
  "commute_balanced_value_sensitive": {init_weight: 0.08}
  # 未覆盖的人设默认 init_weight = 1/30 ≈ 0.0333
```

### YAML4 · scoring_weights.yaml — 评分公式 + 权重 + S/A/P阈值

```yaml
# scoring_weights.yaml
engine_initial_weights:            # 三大引擎初始权重（3Loop会校准）
  identity_signaling: 0.40         # 引擎一·身份表达
  visual_system1: 0.30             # 引擎二·视觉观感
  value_system2: 0.30              # 引擎三·价值评估

channel_initial_weights:           # 双渠道初始权重
  natural: 0.50
  livestream: 0.50

natural_channel_formula:           # 自然流量评分公式
  "0.40 * F06 + 0.30 * F07 + 0.30 * (10 - F03)"

livestream_channel_formula:        # 直播带货评分公式
  "0.35 * F05 + 0.35 * F04 + 0.30 * F10"

grading_thresholds:                # S/A+/A/P分级阈值
  S:    {natural: 7.5, livestream: 7.0, oppose_rate_max: 0.15, vm_min: 0}
  A_plus: {one_dim_min: 7.5, other_dim_range: [6.0, 7.0], upgradeable: true}
  A:    {one_dim_min: 7.0, require_clear_channel: true}
  P:    {brand_tone_min: 7.0, uniqueness_min: 7.5}
  risk: {any_below: 5.0, oppose_rate_over: 0.30}

loop_protection:                   # 3Loop保护机制参数
  MIN_IMPROVEMENT: 0.005           # 最小Spearman提升阈值
  LOOP1_BIAS_CLAMP: [0.7, 1.3]     # Loop1偏置系数夹紧区间
  LOOP2_WEIGHT_MIN_RATIO: 0.5      # Loop2人设最低权重比例(×等权)
  LOOP3_ENGINE_FLOOR: 0.05         # Loop3引擎最低话语权5%
```

### YAML5 · category_registry.yaml — 品类 + 价格带 + 别名

```yaml
# category_registry.yaml
categories:
  jacket_coat:
    name: 夹克外套
    aliases: [外套, 夹克, 风衣, 冲锋衣]   # 自动匹配关键词
    price_distribution:                     # 品类价格分布百分位（从历史订单拟合）
      p25: 199
      p50: 299
      p75: 459
      p90: 699

  t_shirt:
    name: T恤
    aliases: [Tee, T恤衫, 短袖]
    price_distribution:
      p25: 59
      p50: 89
      p75: 129
      p90: 179

  # ... 短裤/长裤/背心/羽绒服/防晒衣/衬衫/卫衣/马甲 同理

# 自动品类识别（没有显式填「品类」列时使用，按版型描述关键词匹配）
auto_detect_keywords:
  jacket_coat: [夹克, 外套, 风衣, 冲锋衣, 防晒衣, 羽绒, 马甲, 棉服]
  t_shirt:     [T恤, Tee, 短袖, tee]
  # ... 其他品类同理
```

### 校准产物目录 `calibrated/`

```
calibrated/
  ├─ loop1_bias_factors.yaml      ← Loop1输出：10特征偏置系数
  ├─ loop2_persona_weights.yaml   ← Loop2输出：30人设真实分布权重
  ├─ loop3_engine_weights.yaml    ← Loop3输出：三大引擎+双渠道权重
  ├─ calibration_report.md        ← 本轮校准报告（含残差款ID列表）
  └─ .gitkeep                     ← 空目录占位
```

下次评估时，系统会优先加载 `calibrated/` 下的产物覆盖初始值，实现"销售数据喂进来→越跑越准"的闭环。

---

> 文档版本：v2.1  
> 创建日期：2026-08-24  
> 通用架构更新：2026-08-27  
> 状态：待用户审阅  
> 下一步：用户确认后进入开发计划讨论
