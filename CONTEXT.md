# 项目领域术语表（fashion-hit-engine）

> 本文件是**术语表，仅此而已**。不含实现细节。
> 目的：让人和 AI 用同一套语言讨论这个系统，避免 20 个词描述 1 个概念。

## 核心概念

### 款式（Style）
一款待评估的服装。Excel 里一行 + 若干张图片。

### 款号（style_id）
款式的唯一编号。图片按文件名前缀与之匹配。

### 品类（category）
Excel 原始品类名（如「外套」），经品牌品类注册表标准化为 category_id。

### FAB描述（fab_description）
款式的版型/面料/功能文字描述，与图片一起喂给 VLM。

### 10特征BARS（structural features）
VLM 对款式图片的 10 个结构化评分（1-10 分）：廓形度、利落感、色彩风险、
功能可见性、上镜感、实穿度、搭配度、面料感知、品牌调性贡献、设计独特性。

### 特征锚定（anchor）
某特征每 2 分一档的行为锚定描述 + 该档位对品牌目标客群的销量暗示。
评分 = 先匹配锚定档位，再判断该档位对销量的贡献。

### 人设（persona）
目标客群细分出的投票者（每品牌 30 个），带身份三轴。

### 身份三轴（identity axes）
场景 × 审美 × 价格。人设的定位坐标系。

### 决策层（decision layer）
品牌的多层购买决策结构。每层有 id/名称/轴键/角色/权重：
- **决策者层**（role=decider）：出钱拍板的人
- **影响层**（role=veto）：能一票否决的人
层级、权重、名称由品牌适配包 YAML 声明，代码不预设角色。
mipo（中大童童装）声明为「妈妈决策者层 + 孩子影响层」；
其他品类可以是「女性买家 + 伴侣意见 + 闺蜜种草」等任意结构。

### 人设投票（persona voting）
每个人设独立给分并说明理由，聚合为加权总分（0-10）。
分数按决策层拆分存储（layer_scores），综合分 = Σ 层分 × 层权重。

### 反对率（opposition rate）
打 <4 分的人设占比。决策者层低分或影响层命中否决线即否决（veto）。

### 双渠道分（natural score / live score）
自然流量预测分与直播带货预测分（0-10）。

### 价格价值匹配（value match）
感知价值 vs 同批价格分位之差。-1~+1，正值=物美价廉。

### 分级（grade）
系统最终输出，5 档：S / A+ / A / P / 风险。

### 相对分级（relative grading）
批次内按综合分分位重定档：S=Top 20%，A+=20-40%，A=40-75%，P=后 25%。
为什么需要：冷启动时 VLM 绝对分尺度未校准（常见全部款 65-75 分 →
绝对档全是 A），但**排序可靠**；人工内审的 S/A/P 本就是批次内相对排序
（这批里最好的打 S），相对分级与人工语义对齐。规则：「风险」款保留
绝对判定不参与分位；同分并列进同一档（分数边界而非排名边界）；幂等
（绝对档快照存 `metadata['absolute_grade']`，重复调用无副作用）。

### 绝对档（absolute grade）
按 `scoring_weights.yaml` 绝对阈值直接切的档位。冷启动时尺度未校准、
参考价值低（批次总表提供对照列 + 分数扎堆告警）；3Loop 校准积累
（≥10 款销量回填）后逐步可用。

### 人工分级（manual grade）
内审人工打的 3 档：S / A / P。与系统分级是**两把不同的尺子**，
对齐规则见 ADR-0001。

### 营销标签（marketing tags）
「是否主推」「是否直播重点」。语义定位见 ADR-0001：它们解释销量，
但不解释款式质量。

### 3Loop校准（three-loop calibration）
- **Loop1**：特征偏置系数（VLM 分数 → 销量的逐特征修正）
- **Loop2**：人设分布权重（哪类人设的票更预测销量）
- **Loop3**：引擎权重 + 双渠道权重（人设/渠道/价格价值怎么合成总分）

### 残差分解（residual decomposition）
预测分 vs 真实销量的偏差诊断：超预期 / 不及预期 / 系统偏差。

### 资源错配归因（marketing attribution）
残差款 × 营销标签（实际投放）的四象限分类：
- **推对了**：投放了且超预期——投放放大了款式潜力
- **漏网爆款**：没推却超预期——本季最大机会损失，下季应提前识别
- **资源错配**：投放了仍不及预期——投放资源浪费在弱款上
- **款式本身弱**：没推且不及预期——残差主要由款式质量解释

前提：营销标签记录**实际投放**，而非计划投放（见 ADR-0001）。

### 盲测组（blind set）
每批次按品类分层随机抽取的对照款式（默认15%）：AI 照常预测并存档，
但结论在内审阶段对运营打码（报告/导出均不可见），内审决策不受 AI 干扰。
销量回填后解锁对照分析：
- **盲测组 Spearman** = AI 预测的「纯净」基线（销量未被 AI 建议放大）
- **AI vs 人工分歧裁决** = 盲测款上 AI 分级与人工分级不一致时，
  按实际销量前/后 1/3 裁决谁对
用途：防止 3Loop 陷入「自己推荐 → 投放放大 → 证明自己准」的自我循环。

### 品牌适配包（brand profile）
`brand_profiles/<brand_id>/` 下的一组 YAML，承载某品牌的全部领域差异：
人设、锚定量表、品类注册表、评分权重、决策结构。品牌差异只走适配包，
不走代码分支。

### 校准产物（calibrated artifacts）
适配包内 `calibrated/` 目录落盘的权重 YAML + 校准报告。可被下一批
预测直接加载，也可整体删除后重训。

### 历史批次（history batch）
跨批次持久化（SQLite + CSV）的款式记录，供 3Loop 累积训练。

### Mock 演示模式
本地确定性生成的假分数，与图片、销量均无关。仅用于调试管线，
不可用于评估准确率。

### 真实 VLM 评估
百炼 API 对图片的实际分析。批次结果带来源标记（模型名/耗时）。
